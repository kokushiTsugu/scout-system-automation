name: GAS Deploy (Matching)

on:
  push:
    paths:
      - 'gas/matching/**'
      - '.github/workflows/gas-matching.yml'

jobs:
  call-deploy:
    uses: ./.github/workflows/gas-deploy.yml
    with:
      dir: gas/matching
